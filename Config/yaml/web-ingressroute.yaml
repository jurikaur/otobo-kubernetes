apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: otobo-web
spec:
  entryPoints:
   - websecure
  routes:
    - match: Host('----')
      kind: Rule
      services:
        - name:  web
          port:  80
